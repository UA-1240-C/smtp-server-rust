[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
concurrent_runtime = { path = "../crates/concurrent_runtime" }
smart_stream = { path = "../crates/smart_stream" }
native-tls = "0.2.7"
